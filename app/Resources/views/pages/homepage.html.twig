{% extends 'base.html.twig' %}

{% block body_class %}home{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .main-wrapper {
            background-image: url('{{ asset('images/gallery/cover.jpg') | imagine_filter('cover') }}');
            background-position: 50%;
            background-size: cover;
            text-align: center;
            min-height: 530px;
            min-height: 60vh;
        }
    </style>
{% endblock %}
